import type { ReactNode } from "react";
import { Boxes, ClipboardCheck, FileText, Gauge, Settings, Truck, UsersRound } from "lucide-react";
import { Sidebar } from "@/components/ui/sidebar";
import { signOut } from "@/lib/auth/actions";

export function AdminShell({ children }: { children: ReactNode }) { const items = [{ label: "Resumen", href: "/admin", icon: Gauge }, { label: "Recepción", href: "/admin/recepcion", icon: ClipboardCheck }, { label: "Bodega", href: "/admin/bodega", icon: Boxes }, { label: "Camiones", href: "/admin/camiones", icon: Truck }, { label: "Facturas", href: "/admin/facturas", icon: FileText }, { label: "Clientes", href: "/admin/clientes", icon: UsersRound }, { label: "Configuración", href: "/admin/configuracion", icon: Settings }]; return <main className="min-h-screen bg-navy-950 p-3 sm:p-5"><div className="mx-auto grid max-w-[1700px] gap-5 lg:grid-cols-[18rem_1fr]"><div className="hidden lg:block"><div className="sticky top-5 h-[calc(100vh-2.5rem)]"><Sidebar items={items} footer={<form action={signOut}><p className="text-sm font-bold">Operaciones A&L</p><button className="mt-3 text-xs font-semibold text-orange-400">Cerrar sesión</button></form>} /></div></div><div className="min-w-0 rounded-[1.8rem] bg-cream-50 p-5 sm:p-8 lg:p-10"><div className="mb-5 flex items-center justify-between lg:hidden"><p className="font-display font-bold">A&L Operaciones</p><select aria-label="Navegación admin" className="rounded-full border border-stone-200 bg-white px-3 py-2 text-sm"><option>Menú</option>{items.map((item) => <option key={item.href}>{item.label}</option>)}</select></div>{children}</div></div></main>; }
