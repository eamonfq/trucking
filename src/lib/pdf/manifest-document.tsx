import { Document, Page, StyleSheet, Text, View } from "@react-pdf/renderer";
import { BOX_CATEGORIES } from "@/lib/config/box-categories";
import type { Box, Truck } from "@/lib/types";

const styles = StyleSheet.create({ page: { padding: 42, fontFamily: "Helvetica", fontSize: 10, color: "#1C2B4B" }, header: { fontSize: 22, fontFamily: "Helvetica-Bold" }, meta: { marginTop: 8, color: "#5A6680" }, row: { flexDirection: "row", justifyContent: "space-between", paddingVertical: 10, borderBottom: "1 solid #E3DED5" }, table: { marginTop: 28 }, total: { marginTop: 24, fontFamily: "Helvetica-Bold", fontSize: 14 } });
export function ManifestDocument({ truck, boxes }: { truck: Truck; boxes: Box[] }) { return <Document title={`Manifiesto ${truck.code}`}><Page size="LETTER" style={styles.page}><Text style={styles.header}>A&L Trucking Logistics · Manifiesto</Text><Text style={styles.meta}>{truck.code} · {truck.route} · {truck.departureDate}</Text><View style={styles.table}>{BOX_CATEGORIES.map((category) => { const count = boxes.filter((box) => box.categoryId === category.id).length; return <View key={category.id} style={styles.row}><Text>{category.name}</Text><Text>{count} cajas · USD {count * category.priceUsd}.00</Text></View>; })}</View><Text style={styles.total}>Total de cajas: {boxes.length}</Text><Text style={styles.meta}>Documento operativo generado para demostración.</Text></Page></Document>; }
