import { SectionTitle } from "@/components/cliente/section-title";
import { WarehouseBoard } from "@/components/admin/warehouse-board";
import { logisticsService } from "@/lib/services/logistics";
export default async function WarehousePage() { const [boxes, trucks, users] = await Promise.all([logisticsService.getBoxes(), logisticsService.getTrucks(), logisticsService.getUsers()]); return <><SectionTitle eyebrow="Inventario operativo" title="Bodega" description="Filtra el inventario y asigna cajas disponibles sin exceder la capacidad del camión." /><div className="mt-7"><WarehouseBoard initialBoxes={boxes.filter((box) => ["recibida", "categorizada", "en-bodega", "excede-categoria"].includes(box.status) && !box.truckId)} initialTrucks={trucks.filter((truck) => ["planificado", "cargando"].includes(truck.status))} users={users} /></div></>; }
