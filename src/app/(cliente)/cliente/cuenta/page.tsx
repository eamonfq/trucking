import Link from "next/link";
import { Shield } from "lucide-react";
import { SectionTitle } from "@/components/cliente/section-title";
import { Card } from "@/components/ui/card";
import { logisticsService } from "@/lib/services/logistics";
export default async function AccountPage() { const user = (await logisticsService.getUsers())[0]!; return <><SectionTitle eyebrow="Perfil" title="Mi cuenta" description="Datos personales del titular del casillero." /><div className="mt-7 grid gap-5 lg:grid-cols-[1fr_.6fr]"><Card className="shadow-none"><dl className="grid gap-5 sm:grid-cols-2"><div><dt className="text-xs text-navy-400">Nombre</dt><dd className="mt-1 font-bold">{user.firstName} {user.paternalLastName} {user.maternalLastName}</dd></div><div><dt className="text-xs text-navy-400">Casillero</dt><dd className="mt-1 font-bold">{user.lockerCode}</dd></div><div><dt className="text-xs text-navy-400">Correo</dt><dd className="mt-1 font-bold">{user.email}</dd></div><div><dt className="text-xs text-navy-400">Teléfono</dt><dd className="mt-1 font-bold">{user.phone}</dd></div></dl></Card><Link href="/cliente/cuenta/seguridad" className="rounded-card bg-navy-950 p-6 text-white"><Shield className="size-6 text-orange-500" /><h2 className="mt-5 font-display text-xl font-bold">Seguridad</h2><p className="mt-2 text-sm leading-6 text-white/55">Actualiza tu contraseña de acceso.</p></Link></div></>; }
