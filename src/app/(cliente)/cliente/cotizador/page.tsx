import { SectionTitle } from "@/components/cliente/section-title";
import { QuoteCalculator } from "@/components/marketing/quote-calculator";
import { configService } from "@/lib/services/config";
export default async function ClientQuotePage() { const [rates,flow] = await Promise.all([configService.getRateTable(),configService.getFlowConfig()]); return <><SectionTitle eyebrow="Herramienta" title="Cotizador de caja" description="Ingresa medidas exteriores y el peso total. Compara cobro por libra, precio fijo y carga especial con el tarifario activo." /><div className="mt-7 rounded-card border border-stone-200 bg-white p-5 shadow-soft sm:p-7"><QuoteCalculator client rates={rates} weightPricing={{pricePerLbUsd:flow.pricePerLbUsd,dimensionalBase:flow.dimensionalBase,dimensionalFactor:flow.dimensionalFactor}} /></div></>; }
